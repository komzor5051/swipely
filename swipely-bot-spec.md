# Swipely Telegram Bot MVP — Product & Technical Specification

## Обзор

Telegram-бот для генерации высококачественных каруселей на основе голосовых сообщений и текста пользователя. На MVP этапе: **красивые фоны + безупречная типографика**, персонаж добавим позже.

Главная цель: **AHA-эффект** за 60 секунд без каких-либо правок. Каждая карусель должна выглядеть так, будто её верстал профессиональный дизайнер.

---

## 1. User Flow (Сценарий использования)

### Шаг 1: Старт бота
```
Юзер → /start
Бот → "Привет! 👋 Я создаю стильные карусели за 1 минуту.
        Наговори или напиши мысль — я упакую её в контент."

Варианты:
- [🎤 Голос]
- [📝 Текст]
```

### Шаг 2: Ввод контента
```
Юзер → Отправляет голосовое сообщение (15-60 секунд) ИЛИ печатает текст

Бот → Активирует Whisper API (для голоса) или напрямую анализирует текст
      "Один момент, анализирую тон твоего голоса..."
      (показать прогресс-бар)
```

### Шаг 3: Выбор стиля
```
Бот → "Вот я услышал. Какой вайб выберешь?"

[🌑 Dark Minimal]  — чёрно-белая, минималистичная типографика
[🌈 Bright & Pop]  — яркие цвета, градиенты, энергичная композиция  
[📰 Newspaper]     — классический стиль, как в журналах
```

### Шаг 4: Генерация & Отправка
```
Бот → ✍️ "Пишу копирайт в твоём стиле..."
      🎨 "Строю макеты..."
      📦 "Собираю итоговую версию..."

[Через 15-20 сек отправляет альбом из 8-10 картинок]
```

### Шаг 5: CTA для монетизации
```
Бот → [Скачать без водяного знака] [Редактировать] [Сохранить стиль]

Если юзер нажал скачать:
  - Если это 1-2й раз → Скачивает свободно
  - Если 3й+ раз → "Нужна подписка для безлимита (490 руб/мес)"
```

---

## 2. Анализ стиля пользователя (Tone of Voice)

### Как работает
1. **Whisper API** → конвертирует голос в текст
2. **Claude анализирует** текст по параметрам:
   - Длина предложений (короткие = рубленый стиль; длинные = академичный)
   - Сленг, любимые словечки, фразеологизмы
   - Тон (дружелюбный, экспертный, саркастичный, мотивирующий)
   - Использование emoji, восклицаний, пауз
3. **Результат сохраняется** в профиль юзера как `tone_template`

### Применение
При каждой генерации этот шаблон подставляется в системный промпт:
```
TONE_GUIDELINES: {
  "sentence_length": "short",
  "tone": "confident_expert",
  "signature_words": ["буквально", "во-первых", "кстати"],
  "emoji_usage": "moderate"
}
```

Это гарантирует, что каждая новая карусель звучит **голосом пользователя**.

---

## 3. Контент-генерация (Claude)

### System Prompt для LLM
```
Ты — эксперт по контент-маркетингу.
Задача: Превратить мысль пользователя в структуру карусели.

OUTPUT ONLY valid JSON:
{
  "slides": [
    {
      "type": "hook",
      "title": "max 7 words",
      "content": "max 28 words",
      "emphasize": ["key_word_1", "key_word_2"]
    },
    {
      "type": "statement",
      "title": "max 7 words",
      "content": "max 28 words",
      "emphasize": []
    }
    ...
  ]
}

SLIDE TYPES:
- hook: первый слайд, главная идея, самая сильная фраза
- statement: подтверждение, усиление хука
- list: список пунктов (макс 3-4 пункта)
- comparison: до/после или контраст
- cta: финальный слайд с призывом

RULES:
- Пиши в стиле {{ TONE_GUIDELINES }}
- Одна мысль на слайд
- Избегай воды и штампов
- Выделяй ключевые слова в "emphasize" для акцента в дизайне
- МАКСИМУМ 28 слов на слайд, оптимально 10-15 слов
```

### Какие параметры приходят в LLM
```
{
  "user_input": "Transcribed or typed text from user",
  "style": "Dark Minimal / Bright & Pop / Newspaper",
  "tone_guidelines": { /* saved from previous inputs */ },
  "slides_count": 8-10
}
```

---

## 4. Архитектура шаблонов (Smart Templates)

Чтобы верстка **никогда не ехала**, используем систему жёстких шаблонов на Canvas/HTML+CSS.

### Типы шаблонов
Каждый шаблон — это файл CSS + HTML-разметка, которая рендерится на сервере.

| Тип | Сетка | Использование |
|-----|-------|---|
| **Full Title** | Заголовок на весь экран (центр) | Hook-слайды |
| **Left Text** | Текст слева, пусто справа | Statement, List |
| **Centered Block** | Текст в белой/цветной плашке в центре | Important quotes |
| **Split Screen** | Два блока текста рядом | Comparison |
| **Bottom CTA** | Текст вверху, CTA кнопка внизу | Final slide |

### Как работает рендер
1. Claude выбирает `template_id` для каждого слайда
2. Бэкенд подставляет текст в шаблон
3. **Satori/Puppeteer** конвертирует HTML в изображение (PNG, 1080x1350 для Instagram)
4. Верстка **никогда** не ехает, потому что CSS жёстко задан

### Контроль объёма текста
```python
def validate_text_length(text, max_length=28):
    words = text.split()
    if len(words) > max_length:
        # Либо обрезаем, либо просим Claude переписать короче
        return truncate_or_regenerate(text, max_length)
    return text
```

---

## 4.1. Правила идеальной верстки текста (дизайнерский уровень)

Цель: любой слайд должен выглядеть так, будто его верстал мидл+/сеньор‑дизайнер. Для этого верстка опирается на золотое сечение, базовую сетку, строгую типографическую иерархию и автоматическую защиту от типографических ошибок.

### 1. Базовая сетка и композиция

- Рабочая область слайда делится вертикально в пропорции 1:1.618 (золотое сечение). Основные текстовые блоки привязываются к этим линиям: левый или правый «столбец» текста + дополнительный воздух со второй стороны.
- По вертикали используется правило третей: ключевые заголовки располагаются в верхней или средней трети, а не прилипают к краям.
- Внутренние отступы (margins) от края холста до текста — не менее 5–7% ширины/высоты кадра, чтобы не было ощущения «зажатого» текста.
- Все текстовые блоки привязаны к единой невидимой сетке (grid): одинаковые отступы между заголовком и текстом, между блоками и объектами на всех слайдах.

### 2. Типографическая иерархия и золотое соотношение размеров

Базовый контекст: карусели читаются в основном с мобильных.

- Размер основного текста (body) для мобильной карусели: 24–28 px.
- Размер заголовка: body_size × 1.618. При 26 px body заголовок ≈ 42 px.
- Подзаголовки и мелкие подписи — примерно body_size / 1.618, то есть 16–18 px.
- Межстрочный интервал (line-height):
  - body: 1.3–1.5 × размер шрифта (легко читается на мобильных).
  - заголовки: 1.1–1.2 × размер шрифта, чтобы крупный текст не «разваливался».
- На одном слайде не более трёх уровней иерархии: заголовок, основной текст, мелкая подпись. Больше уровней создают визуальный шум.

### 3. Выравнивание, «рэг» и композиция текста

- Для абзацев в 2+ строк используется выравнивание по левому краю — это стандарт в digital‑дизайне и даёт аккуратный «рэг» (неровный правый край).
- Центровка применяется только для:
  - коротких hook‑заголовков (до 3–4 слов),
  - очень кратких цитат.
  Длинные абзацы, выровненные по центру, запрещены.
- Каждый текстовый блок должен быть визуально связан:
  - либо с заголовком/подзаголовком,
  - либо с рамкой/плашкой,
  - либо с другими элементами сетки.
  Не допускается «висящий» текст, оторванный от композиции.

### 4. Вдовы, сироты и висячие строки

Вёрстка автоматически избегает типографических ошибок типа widows/orphans:

- Запрещены:
  - последняя строка абзаца из 1–2 очень коротких слов;
  - первая строка абзаца, одиноко оставшаяся на новом блоке/слайде.
- Проверка перед рендером:
  - если длина последней строки < 25% средней длины строки — текст отправляется на автоперефразировку (LLM просится «уплотнить» формулировку) или объединяется с предыдущей строкой.
  - при необходимости между некоторыми словами ставятся неразрывные пробелы, чтобы одно короткое слово не «отваливалось» на отдельную строку.

### 5. Линейки, отступы и белое пространство

- Вокруг каждого текстового блока сохраняется минимальный «воздух»:
  - сверху/снизу — минимум 2 единицы от базового line‑height,
  - между абзацами — 1–1.5 единицы.
- Один слайд никогда не заполняется текстом более чем на 40–50% площади. Если текст не влезает — он автоматически разбивается на дополнительные слайды.
- Вокруг главного заголовка оставляется больше белого пространства, чем вокруг body‑текста, чтобы глаз читал его как главный акцент.

### 6. Контраст и читаемость

- Минимальное соотношение контраста текста и фона — 4.5:1 (WCAG). Это особенно критично для мобильных экранов и просмотра на солнце.
- При сложных или шумных фонах:
  - под текст подставляется полупрозрачная плашка,
  - либо используется лёгкий blur под текстовой областью,
  - либо фон локально затемняется/осветляется.
- Тени у текста:
  - только мягкие, с небольшим радиусом размытия и низкой непрозрачностью,
  - без жёстких «обводок», которые удешевляют визуал.

### 7. Работа с акцентами (emphasize)

- Массив `emphasize` используется строго дозированно:
  - на слайде — не более 3–5 акцентных слов.
- Для акцентных слов:
  - меняется цвет на `accentColor`,
  - size +1–2 px относительно body,
  - font‑weight: 700.
- Не комбинировать более двух типов выделения одновременно (например, цвет + жирный ОК; цвет + жирный + капс + тень — уже перебор).

### 8. Ограничения по объёму текста

- Оптимальный объём — 10–15 слов на слайд; абсолютный максимум — 28 слов.
- Для списков:
  - максимум 3–4 пункта,
  - каждый пункт до 8–10 слов.
- Hook‑слайд:
  - заголовок — до 2 строк (3–5 слов каждая),
  - опционально короткий подзаголовок (до 10–12 слов),
  - никакого длинного body‑текста.

### 9. Автоматическая валидация перед рендером

Перед финальным рендером каждый слайд проходит программный чек‑лист:

1. Проверка лимита слов (общий + по пунктам).
2. Проверка widows/orphans по последней и первой строкам.
3. Проверка, что % занятой текстом площади < 50%.
4. Проверка количества акцентных слов.
5. Проверка контраста текста и фона по формуле luminance (≥ 4.5:1).

Если одно из правил нарушено — текст автоматически отправляется в быстрый rephrase‑запрос к LLM с конкретной инструкцией («сократи на 20%», «объедини последнюю строку с предыдущей», «уменьши число акцентных слов до трёх» и т.п.).

---

## 5. Визуальный дизайн (Графика)

### Фоны в зависимости от стиля

**Dark Minimal:**
- Фон: Чёрный (#0a0a0a) с едва заметным noise (opacity 2-3%)
- Текст: Белый (#FFFFFF), Sans-serif (Montserrat, Inter)
- Акценты: Один яркий цвет (например, неоновый синий #00D9FF)
- Элементы: Минимум графики, только тонкие линии (1-2px) и небольшие точки
- Плашки: Прозрачные с полупрозрачной подложкой (rgba с opacity 0.15-0.2)

**Bright & Pop:**
- Фоны: Генеративные градиенты (от верхнего левого к нижнему правому, 2-3 основных цвета)
- Текст: Чёрный (#1a1a1a) для читаемости на светлых градиентах
- Акценты: 2-3 ярких контрастных цвета (фиолетовый #7C3AED, розовый #EC4899, голубой #06B6D4)
- Элементы: Стрелки (толщина 3-4px), кружки, абстрактные 3D-фигуры (создаются через CSS gradient tricks или SVG)
- Плашки: Белые или цветные с небольшой полупрозрачностью и мягкой тенью (shadow: 0 10px 30px rgba(0,0,0,0.1))

**Newspaper:**
- Фон: Светло-бежевый (#F5F1E8)
- Текст: Чёрный (#1a1a1a), Serif-шрифт (Georgia, Playfair Display для заголовков)
- Акценты: Коричневый (#92400E), приглушённые цвета
- Элементы: Декоративные разделители (горизонтальные линии, орнаменты), номера слайдов в углах, классические кавычки
- Плашки: С тонкой чёрной границей (border: 1px solid #1a1a1a), заполнение либо белое, либо ещё более светлый бежевый

### Типографика (Обязательно)

- **Выделение ключевых слов:** Слова из массива `emphasize` должны быть:
  - Другим цветом (accentColor)
  - Жирным (font-weight: 700)
  - Слегка увеличенным размером (+1–2 px)
  - Может быть небольшой background-color с opacity 0.2–0.3 для лучшей читаемости

- **Иерархия размеров:**
  - Заголовок (title): 42–48px, font-weight: 700, line-height: 1.1–1.2
  - Основной текст: 24–28px, font-weight: 400, line-height: 1.3–1.5
  - Подзаголовок: 16–18px, font-weight: 500, line-height: 1.4

- **Шрифты по стилям:**
  - Dark Minimal: Inter, Montserrat (modern, clean)
  - Bright & Pop: Poppins, Space Grotesk (playful)
  - Newspaper: Georgia, Playfair Display (classic, serif)

---

## 6. Технический стек

### Backend
- **Framework:** Python (FastAPI) или Node.js (Express)
- **LLM для текста:** Claude Haiku или GPT-4o mini
- **Speech-to-Text:** OpenAI Whisper API
- **Image Rendering:** Satori (HTML→PNG) или Puppeteer (Headless Chrome)

### Telegram Integration
- **Library:** `python-telegram-bot` (aiogram) или `telegraf` (Node.js)
- **File Upload:** Прямое сохранение на сервер → генерация → отправка Media Group

### Database
- **User profiles:** PostgreSQL (tone_guidelines, usage_count, subscription_status)
- **Generated carousels:** Cache на 48 часов (Redis)

### API Dependencies
- OpenAI Whisper (голос)
- Claude API (анализ + генерация контента)
- Nanobanana API (будущее: для персонажей)

### Deployment
- Docker контейнер на VPS (например, DigitalOcean, Hetzner)
- Webhook для Telegram (вместо polling)

---

## 7. MVP Scope (Что входит в первый релиз)

✅ Голос + Текст ввод  
✅ Анализ Tone of Voice  
✅ Генерация контента (Claude)  
✅ 3 стиля дизайна (Dark Minimal, Bright, Newspaper)  
✅ Рендеринг картинок (Satori)  
✅ Отправка альбома в Telegram  
✅ Лимит 2 бесплатные генерации → Paywall  
✅ Кнопка «Скачать без водяного знака» → Отправляет готовый файл  
✅ Автоматическая валидация верстки по 9 правилам (раздел 4.1)

❌ Редактирование через чат (V2)  
❌ Персонажи через Nanobanana (V2)  
❌ Кастомизация шрифтов и цветов (V2)  
❌ Рассылка идей для контента (V2)  

---

## 8. Монетизация & Paywall

### Бесплатный тариф
- 2 генерации в неделю
- На всех картинках водяной знак "Powered by Swipely"
- Экспорт только в среднем качестве (720p)

### Платный тариф (490 руб/мес или $9.99/мес)
- ∞ генерации
- Без водяного знака
- HD-экспорт (1080p)
- Ускоренная генерация (5 сек вместо 20)
- Сохранение всех стилей в личной библиотеке

### CTA Моменты
1. **После 2й бесплатной генерации:** 
   ```
   "Лимит исчерпан, но ты можешь пригласить друга 
   и получить +3 генерации бесплатно [Ссылка]"
   ```

2. **При скачивании 3й генерации:**
   ```
   [Скачать без водяного знака за 490 руб/мес?]
   [Нет, скачать в низком качестве]
   ```

---

## 9. KPI & Метрики для отслеживания

| Метрика | Целевое значение | Комментарий |
|---------|------------------|-----------|
| DAU (Daily Active Users) | +10% в неделю | Запуск в первую неделю |
| Avg time in bot | >2 мин | Признак engagement |
| Conversion to paid | ≥ 5-10% | После 2й генерации |
| Churn rate | < 5% в месяц | Платные юзеры |
| NPS | > 40 | Качество результата |
| Quality score | ≥ 85% | Слайды без ошибок верстки |

---

## 10. Timeline к запуску

| Неделя | Что | Кто |
|--------|-----|-----|
| 1-2 | Backend + Telegram Bot skeleton | Backend dev |
| 2-3 | Claude integration + Whisper | Backend dev |
| 3-4 | Templates & Satori rendering + валидация | Frontend/Backend |
| 4-5 | Тестирование MVP на 50 юзерах | Весь тим |
| 5 | Launch в Telegram | Growth |

---

## 11. Возможные проблемы & Решения

### Проблема: ИИ генерирует слишком длинный текст
**Решение:** 
- Жёсткий лимит в промпте: "МАКСИМУМ 28 слов на слайд"
- Если LLM превышает — автоматически обрезаем или требуем переписать
- Валидация перед рендером (чек-лист из раздела 4.1)

### Проблема: Верстка ехает на разных экранах
**Решение:**
- Используем Satori или Puppeteer (не в браузере на клиенте)
- Жёстко заданные CSS-гриды для каждого шаблона
- Тестирование на 1080x1350 (Instagram ratio)
- Все тексты проходят через функцию линейного позиционирования (никакого абсолютного позиционирования)

### Проблема: Видны вдовы/сироты или перекос текста
**Решение:**
- Auto-rephrase функция в LLM при обнаружении нарушения по правилам раздела 4.1
- Неразрывные пробелы между короткими словами и знаками препинания
- Контроль line-break поведения через CSS свойства (word-break, overflow-wrap и т.п.)

### Проблема: Контраст текста на некоторых фонах недостаточен
**Решение:**
- Автоматическая проверка WCAG контраста (4.5:1) перед отправкой
- Если контраст < 4.5:1 — подставляется полупрозрачная плашка под текст
- На Bright & Pop фоне всегда используется тёмный текст; на Dark Minimal — светлый

---

## 12. Будущие фичи (Roadmap)

- **V2:** Редактирование в чате ("Смени заголовок на 3-м слайде")
- **V2:** Персонажи + Nanobanana integration
- **V2:** Шаблоны под разные платформы (TikTok 9:16, LinkedIn 1200x627)
- **V2:** Генеративные фоны через Stable Diffusion
- **V3:** API для других сервисов (например, для ведения соцсетей)
- **V3:** Community-галерея лучших каруселей

---

**Готово к разработке. Запускаем в Telegram!** 🚀
