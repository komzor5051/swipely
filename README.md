# 🤖 Swipely Telegram Bot

Telegram-бот для генерации высококачественных каруселей из голоса или текста с использованием AI.

## 🎯 Возможности

- **Текстовый ввод**: Прямой ввод текста ✅
- **Голосовой ввод**: Транскрибация через Whisper API (опционально)
- **3 дизайн-пресета**:
  - 🌸 **Minimal Pop** - минималистичный стиль с розовыми акцентами ✅
  - 📓 **Notebook Sketch** - стиль блокнота с сеткой (в разработке)
  - 🌑 **Darkest Hour** - темный профессиональный стиль (в разработке)
- **AI-генерация контента**: Claude 3.5 Haiku через OpenRouter ✅
- **Автоматический рендеринг**: HTML → PNG через Puppeteer ✅
- **Лимиты и монетизация**: 2 бесплатные генерации/неделю ✅

## 📋 Требования

### Обязательно:
- Node.js 16+
- npm или yarn
- **OpenRouter API ключ** (для Claude) - ОБЯЗАТЕЛЬНО
- Telegram Bot Token (уже настроен)

### Опционально:
- OpenAI API ключ (для голосового ввода) - если нет, работает только с текстом

## ⚡ Быстрый старт (3 шага)

### Минимальная настройка для тестирования:

1. **Установить зависимости:**
   ```bash
   cd swipely-bot
   npm install
   ```

2. **Добавить OpenRouter ключ в `.env`:**
   ```bash
   OPENROUTER_API_KEY=sk-or-v1-ваш_ключ_здесь
   ```
   (Получить на https://openrouter.ai/)

3. **Запустить:**
   ```bash
   npm run dev
   ```

Готово! Бот работает с текстовым вводом ✅

---

## 🚀 Полная установка

### 1. Клонирование и установка зависимостей

```bash
cd swipely-bot
npm install
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Заполните переменные окружения в `.env`:

```env
# Telegram Bot Token
TELEGRAM_BOT_TOKEN=your_bot_token_here

# OpenRouter API для Claude (ОБЯЗАТЕЛЬНО)
OPENROUTER_API_KEY=your_openrouter_key_here

# OpenAI API для Whisper (ОПЦИОНАЛЬНО)
# Оставьте пустым если хотите работать только с текстом
OPENAI_API_KEY=

# Supabase (для production)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_service_role_key_here
```

#### Где получить API ключи:

**OpenRouter API** (ОБЯЗАТЕЛЬНО - для генерации контента):
1. Зарегистрируйтесь на https://openrouter.ai/
2. Пополните баланс ($5 минимум, хватит на ~500 генераций)
3. Перейдите в раздел **Keys**
4. Скопируйте API ключ и вставьте в `.env`

**OpenAI API** (ОПЦИОНАЛЬНО - только если нужен голосовой ввод):
1. Зарегистрируйтесь на https://platform.openai.com/
2. Создайте API ключ в разделе API Keys
3. Пополните баланс ($5 минимум)
4. Вставьте ключ в `.env` (или оставьте пустым для работы только с текстом)

### 3. Запуск бота

#### Development режим (с автоперезагрузкой):

```bash
npm run dev
```

#### Production режим:

```bash
npm start
```

## 📁 Структура проекта

```
swipely-bot/
├── src/
│   ├── index.js                 # Главный файл бота
│   ├── handlers/                # Обработчики команд (TODO)
│   ├── services/
│   │   ├── database.js          # SQLite база данных
│   │   ├── claude.js            # Генерация контента через Claude
│   │   ├── whisper.js           # Транскрибация голоса
│   │   └── renderer.js          # Рендеринг HTML → PNG
│   ├── templates/
│   │   ├── minimal_pop.html     # Шаблон Minimal Pop
│   │   ├── notebook.html        # Шаблон Notebook (TODO)
│   │   └── darkest.html         # Шаблон Darkest Hour (TODO)
│   └── utils/                   # Утилиты (TODO)
├── data/
│   └── swipely.db               # SQLite БД (создастся автоматически)
├── output/                      # Сгенерированные изображения
├── temp/                        # Временные файлы (голос)
├── .env                         # Конфигурация
└── package.json
```

## 🎨 Дизайн-пресеты

### 1. Minimal Pop
- Цвета: Розовый (#FF0080) + Cyan (#00BCD4) + Белый
- Шрифты: Roboto Light, Inter
- Стиль: Воздушный минимализм

### 2. Notebook Sketch
- Цвета: Бежевый (#F5E6D3) + Оранжевый (#FF5722)
- Фон: Клетка как в тетради
- Шрифты: Playfair Display, Courier New
- Стиль: Рукописные заметки

### 3. Darkest Hour
- Цвета: Черный (#000000) + Оранжевый (#FF5500)
- Шрифты: Playfair Display (serif) + Inter (sans)
- Стиль: Профессиональный темный

## 💾 База данных

SQLite база с двумя таблицами:

**users**:
- `user_id` - Telegram ID
- `username` - Никнейм
- `subscription_tier` - 'free' | 'pro'
- `generation_count` - Счетчик генераций
- `last_generation_date` - Дата последней генерации
- `tone_guidelines` - JSON с стилем речи пользователя

**generations**:
- История генераций для статистики

## 🔧 Как это работает

### User Flow:

1. Пользователь отправляет `/start`
2. Выбирает: голос или текст
3. Отправляет контент
4. Бот транскрибирует голос (если нужно)
5. Показывает выбор стиля (3 кнопки)
6. Генерирует контент через Claude
7. Рендерит 8-10 слайдов в PNG
8. Отправляет альбомом в Telegram

### Технический стек:

- **Telegraf**: Telegram Bot API
- **OpenRouter**: Доступ к Claude 3.5 Haiku
- **OpenAI Whisper**: Распознавание голоса
- **Puppeteer**: Рендеринг HTML → PNG
- **better-sqlite3**: Локальная БД
- **axios**: HTTP запросы

## 📊 Монетизация

### Бесплатный тариф:
- 5 генераций в месяц
- Водяной знак "Powered by Swipely"
- Стандартное качество

### PRO тариф (790₽/мес):
- 50 генераций в месяц
- Без водяного знака
- HD качество (1080p)
- Ускоренная генерация
- Сохранение стилей

## 🚀 Деплой на сервер (VPS)

### Вариант 1: PM2 (рекомендуется)

1. **Подключитесь к VPS через SSH:**
   ```bash
   ssh root@your-server-ip
   ```

2. **Установите Node.js 18+:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

3. **Установите PM2:**
   ```bash
   sudo npm install -g pm2
   ```

4. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/yourusername/swipely-telegram-bot.git
   cd swipely-telegram-bot
   ```

5. **Установите зависимости:**
   ```bash
   npm install
   ```

6. **Настройте .env:**
   ```bash
   cp .env.example .env
   nano .env
   # Вставьте ваши API ключи и сохраните (Ctrl+X, Y, Enter)
   ```

7. **Настройте Supabase:**
   - Выполните SQL скрипт `fix-bot-rls-policies.sql` в Supabase SQL Editor
   - Убедитесь что используете `service_role` key в .env

8. **Запустите бота с PM2:**
   ```bash
   pm2 start src/index.js --name swipely-bot
   pm2 save
   pm2 startup
   ```

9. **Проверьте статус:**
   ```bash
   pm2 status
   pm2 logs swipely-bot
   ```

### Вариант 2: systemd service

1. Создайте systemd unit файл:
   ```bash
   sudo nano /etc/systemd/system/swipely-bot.service
   ```

2. Вставьте конфигурацию:
   ```ini
   [Unit]
   Description=Swipely Telegram Bot
   After=network.target

   [Service]
   Type=simple
   User=root
   WorkingDirectory=/root/swipely-telegram-bot
   ExecStart=/usr/bin/node src/index.js
   Restart=always
   RestartSec=10
   Environment=NODE_ENV=production

   [Install]
   WantedBy=multi-user.target
   ```

3. Запустите сервис:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable swipely-bot
   sudo systemctl start swipely-bot
   sudo systemctl status swipely-bot
   ```

### Вариант 3: Docker (если есть Docker на сервере)

1. Создайте Dockerfile (уже в проекте)
2. Соберите образ:
   ```bash
   docker build -t swipely-bot .
   ```
3. Запустите контейнер:
   ```bash
   docker run -d --name swipely-bot --env-file .env swipely-bot
   ```

### Мониторинг

**PM2:**
```bash
pm2 logs swipely-bot       # Логи в реальном времени
pm2 status                 # Статус всех процессов
pm2 restart swipely-bot    # Перезапуск
pm2 stop swipely-bot       # Остановка
```

**systemd:**
```bash
sudo journalctl -u swipely-bot -f    # Логи в реальном времени
sudo systemctl status swipely-bot    # Статус
sudo systemctl restart swipely-bot   # Перезапуск
```

### Обновление на сервере

```bash
cd swipely-telegram-bot
git pull
npm install
pm2 restart swipely-bot
# или: sudo systemctl restart swipely-bot
```

## 🐛 Troubleshooting

### Ошибка: "OPENROUTER_API_KEY не настроен"
Проверьте что в `.env` указан правильный ключ.

### Ошибка: "Не удалось распознать голос"
Проверьте что OpenAI API ключ верный и есть баланс.

### Puppeteer не запускается
Установите зависимости:
```bash
# macOS
brew install chromium

# Linux (Ubuntu/Debian)
sudo apt-get install -y chromium-browser
```

### База данных не создается
Убедитесь что папка `data/` существует:
```bash
mkdir -p data
```

## 📝 TODO (Next Steps)

- [ ] Создать шаблоны Notebook и Darkest Hour
- [ ] Добавить tone of voice анализ
- [ ] Интегрировать платежную систему
- [ ] Добавить водяные знаки для free пользователей
- [ ] Реализовать кнопку "Редактировать"
- [ ] Добавить реферальную систему
- [ ] Создать админ-панель

## 📞 Поддержка

По вопросам: Telegram @lvmn_creator

## 📄 Лицензия

MIT
