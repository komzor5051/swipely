---
phase: 05-credits-based-free-tier-chat-lead-magnet-landing-analyze-analyze-profile-drift-wow-onboarding-posthog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - swipely-nextjs/lib/services/profile-scraper.ts
  - swipely-nextjs/lib/services/__tests__/profile-scraper.test.ts
  - swipely-nextjs/app/api/profile-audit/route.ts
  - swipely-nextjs/app/api/profile-audit/__tests__/route.test.ts
  - swipely-nextjs/app/api/ai/profile-audit/route.ts
  - swipely-nextjs/lib/services/profile-recommendations.ts
autonomous: true
requirements: [DRIFT-01, DRIFT-02]

must_haves:
  truths:
    - "scrapeProfile() reads APIFY_API_KEY env var (not APIFY_API_TOKEN)"
    - "POST /api/profile-audit returns 200 for instagram.com URLs in production"
    - "/api/ai/profile-audit and /api/profile-audit return compatible shapes (no field drift)"
  artifacts:
    - path: "swipely-nextjs/lib/services/profile-scraper.ts"
      provides: "Multi-platform scraper using canonical APIFY_API_KEY env var"
    - path: "swipely-nextjs/lib/services/__tests__/profile-scraper.test.ts"
      provides: "Unit test asserting APIFY_API_KEY usage"
    - path: "swipely-nextjs/app/api/profile-audit/__tests__/route.test.ts"
      provides: "Integration test for /api/profile-audit happy path"
  key_links:
    - from: "lib/services/profile-scraper.ts"
      to: "process.env.APIFY_API_KEY"
      via: "direct env read"
      pattern: "process\\.env\\.APIFY_API_KEY"
---

<objective>
Fix the analyze_profile drift: env var name mismatch in profile-scraper.ts (line 59-60 reads APIFY_API_TOKEN, canonical name is APIFY_API_KEY per .env and apify.ts:32) and align the two parallel audit routes so their output shapes do not drift further.

Purpose: Unblocks chat audit for Instagram/Threads/LinkedIn URLs (currently throws "APIFY_API_TOKEN not configured"). Prerequisite for /analyze landing (05-04).
Output: Working multi-platform audit + regression tests preventing future drift.
</objective>

<execution_context>
@/Users/lvmn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lvmn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-credits-based-free-tier-chat-lead-magnet-landing-analyze-analyze-profile-drift-wow-onboarding-posthog/05-RESEARCH.md
@swipely-nextjs/lib/services/profile-scraper.ts
@swipely-nextjs/lib/services/apify.ts
@swipely-nextjs/lib/services/profile-recommendations.ts
@swipely-nextjs/app/api/profile-audit/route.ts
@swipely-nextjs/app/api/ai/profile-audit/route.ts

<interfaces>
From lib/services/apify.ts:32 (canonical):
  const key = process.env.APIFY_API_KEY;
  if (!key) throw new ApifyError("APIFY_API_KEY not configured");

From lib/services/profile-scraper.ts:59 (BUG):
  const token = process.env.APIFY_API_TOKEN;  // WRONG — should be APIFY_API_KEY
  if (!token) throw new Error("APIFY_API_TOKEN not configured");
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Write failing tests for env var + audit route shape</name>
  <files>swipely-nextjs/lib/services/__tests__/profile-scraper.test.ts, swipely-nextjs/app/api/profile-audit/__tests__/route.test.ts</files>
  <read_first>
    - swipely-nextjs/lib/services/profile-scraper.ts (current implementation, line 59-60 bug)
    - swipely-nextjs/lib/services/apify.ts (canonical APIFY_API_KEY pattern)
    - swipely-nextjs/app/api/profile-audit/route.ts (current chat route)
    - swipely-nextjs/app/api/ai/profile-audit/route.ts (onboarding route, different shape)
    - swipely-nextjs/vitest.config.ts (test runner config)
    - swipely-nextjs/lib/analytics/__tests__ (reference test patterns)
  </read_first>
  <behavior>
    - Test 1 (profile-scraper.test.ts): When APIFY_API_KEY is set and APIFY_API_TOKEN is unset, calling scrapeProfile("https://instagram.com/test") must NOT throw "APIFY_API_TOKEN not configured". Use vi.mock to stub fetch to Apify.
    - Test 2 (profile-scraper.test.ts): When APIFY_API_KEY is unset, scrapeProfile must throw with message containing "APIFY_API_KEY" (not "APIFY_API_TOKEN").
    - Test 3 (profile-scraper.test.ts): detectPlatform returns "instagram" | "threads" | "linkedin" | "telegram" for representative URLs.
    - Test 4 (route.test.ts): POST /api/profile-audit with valid Instagram URL and mocked scrapeProfile + generateProfileRecommendations returns 200 with body containing keys: summary, strengths, weaknesses, ideas.
    - Test 5 (route.test.ts): POST /api/profile-audit with invalid URL returns 400.
  </behavior>
  <action>
    Create swipely-nextjs/lib/services/__tests__/profile-scraper.test.ts using vitest. Use `vi.stubEnv("APIFY_API_KEY", "test-key")` and `vi.unstubAllEnvs()` in beforeEach/afterEach. Mock global fetch via `vi.stubGlobal("fetch", vi.fn().mockResolvedValue({ ok: true, json: async () => ([{ ...mockApifyProfile }]) }))`. Test that calling scrapeProfile("https://instagram.com/test") does not throw "APIFY_API_TOKEN not configured" and consumed env is APIFY_API_KEY.

    Create swipely-nextjs/app/api/profile-audit/__tests__/route.test.ts. Mock @/lib/services/profile-scraper and @/lib/services/profile-recommendations. Import { POST } from "../route". Call POST with a NextRequest constructed via `new Request("http://localhost/api/profile-audit", { method: "POST", body: JSON.stringify({ url: "https://instagram.com/test" }) })`. Assert response.status === 200 and parsed body has keys summary, strengths, weaknesses, ideas.

    Run tests — they MUST fail (RED) because profile-scraper.ts still reads APIFY_API_TOKEN.
  </action>
  <verify>
    <automated>cd swipely-nextjs && npm test -- --run lib/services/__tests__/profile-scraper.test.ts app/api/profile-audit/__tests__/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `swipely-nextjs/lib/services/__tests__/profile-scraper.test.ts` exists.
    - File `swipely-nextjs/app/api/profile-audit/__tests__/route.test.ts` exists.
    - `grep -c "APIFY_API_KEY" swipely-nextjs/lib/services/__tests__/profile-scraper.test.ts` returns >= 2.
    - `npm test` for these two files reports at least 1 FAILED test (RED state — proving they test current broken behavior).
  </acceptance_criteria>
  <done>Tests exist, run, and fail with messages referencing the APIFY_API_TOKEN bug.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix env var bug + merge audit route shapes (GREEN)</name>
  <files>swipely-nextjs/lib/services/profile-scraper.ts, swipely-nextjs/app/api/profile-audit/route.ts, swipely-nextjs/app/api/ai/profile-audit/route.ts</files>
  <read_first>
    - swipely-nextjs/lib/services/profile-scraper.ts (lines 55-70 — the bug site)
    - swipely-nextjs/app/api/profile-audit/route.ts (full file — chat target)
    - swipely-nextjs/app/api/ai/profile-audit/route.ts (full file — onboarding target with different shape)
    - swipely-nextjs/lib/services/profile-recommendations.ts (generateProfileRecommendations output shape)
    - swipely-nextjs/lib/services/profile-audit.ts (auditProfile — onboarding-only output shape)
    - swipely-nextjs/components/dashboard/conv/types.ts (AuditReportData UI contract)
  </read_first>
  <action>
    Step 1 (the actual bug): In swipely-nextjs/lib/services/profile-scraper.ts, replace line 59 `const token = process.env.APIFY_API_TOKEN;` with `const token = process.env.APIFY_API_KEY;`. Replace line 60 `if (!token) throw new Error("APIFY_API_TOKEN not configured");` with `if (!token) throw new Error("APIFY_API_KEY not configured");`. Do NOT rename the local variable `token` — keep it as `token` so downstream usage is untouched.

    Step 2 (drift defense): At the top of swipely-nextjs/app/api/ai/profile-audit/route.ts, add the comment block:
      ```
      // INTERNAL — onboarding-only. Returns { niche, tone, audience, visual_style, suggested_topic }.
      // For the chat-tool audit (AuditReportData shape: summary/strengths/weaknesses/ideas) use /api/profile-audit.
      // Do NOT merge these two routes without updating both consumers (onboarding page + chat audit_report ui_hint).
      // See .planning/phases/05-.../05-RESEARCH.md Pitfall 3.
      ```

    Step 3 (defensive): In swipely-nextjs/app/api/profile-audit/route.ts, ensure the response body always includes exactly the keys `{ summary, strengths, weaknesses, ideas }` (matching AuditReportData). If generateProfileRecommendations returns extra fields, pick only those four into the response.

    Run tests from Task 1 — they MUST now pass (GREEN).
  </action>
  <verify>
    <automated>cd swipely-nextjs && npm test -- --run lib/services/__tests__/profile-scraper.test.ts app/api/profile-audit/__tests__/route.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "APIFY_API_TOKEN" swipely-nextjs/lib/services/profile-scraper.ts` returns 0.
    - `grep -c "APIFY_API_KEY" swipely-nextjs/lib/services/profile-scraper.ts` returns >= 2 (env read + error message).
    - `grep "INTERNAL — onboarding-only" swipely-nextjs/app/api/ai/profile-audit/route.ts` returns the comment line.
    - All tests from Task 1 PASS.
    - `cd swipely-nextjs && npx tsc --noEmit` exits 0 (no type regressions).
  </acceptance_criteria>
  <done>profile-scraper.ts uses APIFY_API_KEY; both audit routes documented to prevent further drift; tests green.</done>
</task>

</tasks>

<verification>
- `grep -rn "APIFY_API_TOKEN" swipely-nextjs/` returns no results (or only in tests/comments documenting the old bug).
- `npm test -- --run` exits 0 with no new failures introduced.
- Manual smoke (post-deploy): POST to /api/profile-audit with `{url:"https://instagram.com/cristiano"}` returns 200 with audit report shape.
</verification>

<success_criteria>
- DRIFT-01: profile-scraper.ts reads APIFY_API_KEY (verified by grep).
- DRIFT-02: Both audit routes have stable, documented shapes; comment block prevents accidental merge without consumer updates.
- Tests cover the bug — regression caught on PR.
</success_criteria>

<output>
After completion, create `.planning/phases/05-credits-based-free-tier-chat-lead-magnet-landing-analyze-analyze-profile-drift-wow-onboarding-posthog/05-01-SUMMARY.md`
</output>
