# ✅ Новый UX готов!

## 🎉 Что сделано:

### 1. Полностью переписан бот:
- ✅ **src/index.js** (520 строк) - новая логика с онбордингом
- ✅ **src/utils/copy.js** - централизованные копирайты
- ✅ **src/data/demoCarousel.js** - статичная демо-карусель для AHA-момента
- ✅ **src/services/tovAnalyzer.js** - анализ Tone of Voice через Claude
- ✅ **src/services/supabaseService.js** - добавлены функции онбординга
- ✅ **supabase-onboarding-schema.sql** - SQL миграция для новых полей

### 2. Новый User Flow:

```
/start
  ├─→ Новый пользователь:
  │   ├─→ Welcome Screen (3 кнопки)
  │   │   ├─→ 🚀 Создать пробную карусель (мгновенная демо)
  │   │   ├─→ 💡 Как это работает?
  │   │   └─→ 🎨 Посмотреть стили
  │   │
  │   ├─→ Онбординг (3 фазы):
  │   │   ├─→ Phase 1: Контекст (кто ты и чем занимаешься?)
  │   │   ├─→ Phase 2: ToV анализ (пример текста → Claude)
  │   │   └─→ Phase 3: Выбор роли (Эксперт/Визионер/Друг)
  │   │
  │   └─→ Завершение → Главное меню
  │
  └─→ Повторный пользователь:
      └─→ Главное меню (Отправь идею)
```

### 3. Ключевые фичи:
- ✨ Демо-карусель за 3 секунды (без AI)
- 🎯 Персонализация через ToV профиль
- 🎭 3 роли: Эксперт, Визионер, Друг
- 📝 Поддержка текста и голоса
- ❌ Удален функционал ссылок
- 💬 Все копирайты из UX-гайда

## 🚀 Запуск:

### Шаг 1: Выполнить SQL миграцию в Supabase

1. Откройте: https://supabase.com/dashboard/project/ijmevkzcpsipyuufjemg/sql

2. Скопируйте содержимое **`supabase-onboarding-schema.sql`**

3. Вставьте в SQL Editor и нажмите **Run**

4. Должно появиться: "Onboarding schema успешно обновлена!"

### Шаг 2: Перезапустить бота

```bash
# Остановить старого бота
pkill -f "node src/index.js"

# Запустить нового
cd "/Users/lvmn/Desktop/ai projects /swipely /swipely-bot"
npm start
```

Или если используется PM2:
```bash
pm2 restart swipely-bot
# или
pm2 stop swipely-bot && pm2 start src/index.js --name swipely-bot
```

### Шаг 3: Протестировать

1. Отправьте `/start` боту в Telegram
2. Должен показаться новый Welcome Screen с 3 кнопками
3. Нажмите "🚀 Создать пробную карусель" - мгновенная демо
4. Пройдите онбординг (3 фазы)
5. Создайте реальную карусель

## 📊 Что изменилось в коде:

### Новые импорты:
```javascript
const copy = require('./utils/copy');
const demoCarousel = require('./data/demoCarousel');
const { analyzeToneOfVoice, formatTovProfile } = require('./services/tovAnalyzer');
const {
  checkOnboardingStatus,
  saveUserContext,
  saveTovProfile,
  completeOnboarding,
  skipOnboarding
} = require('./services/supabaseService');
```

### Новые callback queries:
- `demo_carousel` - демо-карусель
- `how_it_works` - как работает бот
- `view_styles` - просмотр стилей
- `start_onboarding` - начало онбординга
- `skip_onboarding` - пропуск онбординга
- `role_expert` / `role_visionary` / `role_friend` - выбор роли

### Новая логика text handler:
- Проверка фазы онбординга
- Phase 1: сохранение контекста
- Phase 2: ToV анализ через Claude
- Phase 3: выбор роли (через callback)
- Обычный флоу только после завершения онбординга

## 📁 Новая структура файлов:

```
swipely-bot/
├── src/
│   ├── data/
│   │   └── demoCarousel.js          # Статичная демо-карусель
│   ├── utils/
│   │   └── copy.js                  # Все копирайты бота
│   ├── services/
│   │   ├── tovAnalyzer.js           # ToV анализ через Claude
│   │   ├── supabaseService.js       # + функции онбординга
│   │   └── ...
│   └── index.js                     # Полностью переписан (520 строк)
├── supabase-onboarding-schema.sql   # SQL миграция
├── UPDATE_GUIDE.md                  # Гайд по обновлению
└── НОВЫЙ_UX_ГОТОВ.md                # Этот файл
```

## 🔧 Troubleshooting:

### Ошибка: Cannot find module './utils/copy'
```bash
# Проверьте что файлы созданы:
ls -la src/utils/copy.js
ls -la src/data/demoCarousel.js
ls -la src/services/tovAnalyzer.js
```

### Ошибка: column "onboarding_completed" does not exist
SQL миграция не выполнена. Выполните `supabase-onboarding-schema.sql` в Supabase.

### Бот не отвечает
```bash
# Проверьте логи:
pm2 logs swipely-bot
# или
tail -f logs/combined.log
```

### Демо-карусель не генерируется
Проверьте что `renderSlides()` работает:
```bash
# В логах должно быть:
# ✨ Генерирую пробную карусель...
# 🎨 Строю макеты...
```

## 📤 Запушить в GitHub:

```bash
cd "/Users/lvmn/Desktop/ai projects /swipely /swipely-bot"

# Добавить новые файлы
git add .

# Коммит
git commit -m "feat: Add onboarding system with ToV analysis

- New UX with 3-phase onboarding
- Phase 1: User context collection
- Phase 2: Tone of Voice analysis via Claude
- Phase 3: Role selection (Expert/Visionary/Friend)
- Instant demo carousel for AHA moment
- Centralized copy.js for all bot texts
- New SQL schema for onboarding fields
- Removed link functionality (text + voice only)"

# Пуш в GitHub
git remote add origin https://github.com/komzor5051/swipely.git
git push -u origin main
```

Если remote уже добавлен:
```bash
git push origin main
```

## 🎯 Следующие шаги:

После успешного тестирования:

1. **Добавить превью стилей** в `view_styles` handler
2. **Реализовать random_topic** - генерация случайной темы
3. **Добавить команду /profile** - просмотр ToV профиля
4. **Добавить команду /settings** - изменение роли и контекста
5. **Интегрировать платежи** YooKassa
6. **Оптимизировать промпты** Claude для снижения стоимости

## 📝 Изменения в UX:

### Было:
```
/start → "Привет, отправь текст" → Выбор слайдов → Стиль → Генерация
```

### Стало:
```
/start → Welcome Screen (3 кнопки) → Демо ИЛИ Онбординг → Создание
```

### Выигрыш:
- ⚡ AHA-момент за 3 секунды (демо-карусель)
- 🎯 Персонализация через ToV
- 💬 Копирайты в стиле "Helpful + Playful"
- 🚫 No Dark Patterns - всё прозрачно

---

**Готово! Бот полностью переписан согласно UX-гайду.**

Теперь:
1. Выполни SQL миграцию
2. Перезапусти бота
3. Протестируй `/start`
4. Запушь в GitHub

Удачи! 🚀
