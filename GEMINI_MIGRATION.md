# ✅ Переход на Google Gemini API

## 🎉 Что изменилось:

### Claude → Gemini
Бот **полностью переведен** с Claude (OpenRouter) на Google Gemini API.

**Преимущества:**
- ⚡ **Быстрее** - gemini-2.0-flash-exp работает моментально
- 💰 **Дешевле** - бесплатный tier: 15 запросов/минуту
- 🌍 **Прямой доступ** - без посредников (OpenRouter)
- 📊 **Лучшая аналитика** - в Google AI Studio видна вся статистика

---

## 📁 Измененные файлы:

### 1. Новые файлы:
- ✅ **src/services/gemini.js** - сервис для работы с Gemini API
  - `generateCarouselContent()` - генерация карусели
  - `analyzeToneOfVoice()` - анализ ToV
  - Поддержка 4 моделей: flash-2.0-exp, flash-1.5, flash-8b, pro-1.5

### 2. Обновленные файлы:
- ✅ **src/index.js** - импорт изменен с `claude.js` → `gemini.js`
- ✅ **src/services/tovAnalyzer.js** - переписан на Gemini API
- ✅ **.env** - добавлен `GOOGLE_GEMINI_API_KEY`

### 3. Старые файлы (можно удалить):
- ❌ **src/services/claude.js** - больше не используется
- ⚠️ `OPENROUTER_API_KEY` в .env можно удалить (но оставлен для совместимости)

---

## 🔑 API Ключи:

### Добавлено в .env:
```env
GOOGLE_GEMINI_API_KEY=AIzaSyBKSJlbyxZayaOKLbkX_HdNOEB6Mbw2gvY
```

**Где взять новый ключ:**
https://aistudio.google.com/apikey

---

## 🤖 Модели Gemini:

### По умолчанию используется:
```javascript
gemini-2.0-flash-exp
```

**Почему эта модель:**
- Самая новая (2.0)
- БЕСПЛАТНАЯ в тестовом режиме
- Быстрая генерация (~2-3 секунды)
- Хорошо работает с русским языком

### Другие доступные модели:
1. **gemini-2.0-flash-exp** - самая новая, быстрая, БЕСПЛАТНАЯ ✅
2. **gemini-1.5-flash** - стабильная версия
3. **gemini-1.5-flash-8b** - очень легкая ("nanobanana")
4. **gemini-1.5-pro** - мощная, для сложных задач

---

## 📊 Промпт:

Промпт **идентичен** тому что был у Claude:
- Вирусные заголовки (3-6 слов)
- Триггерные формулы
- Tone of Voice адаптация
- Структура слайдов (hook, statement, list, cta)

**Качество генерации:** ожидается такое же или лучше, чем у Claude.

---

## 🧪 Тестирование:

После запуска бота проверить:
1. ✅ Демо-карусель (статичная, без AI)
2. ✅ Онбординг Phase 2: ToV анализ через Gemini
3. ✅ Генерация реальной карусели через Gemini
4. ✅ Качество текста (сравнить с Claude)

---

## 💰 Стоимость:

### Claude (через OpenRouter):
- ~15₽ за генерацию карусели (5 слайдов)
- ~3₽ за ToV анализ

### Gemini:
- **БЕСПЛАТНО** до лимитов:
  - 15 запросов/минуту
  - 1500 запросов/день
  - 1M токенов/месяц

**Экономия:** ~100% на API costs (пока в free tier)

---

## 🚀 Следующие шаги:

1. Запустить бота: `npm start`
2. Протестировать генерацию
3. Сравнить качество с Claude
4. Если качество хуже - можно переключиться на `gemini-1.5-pro`

---

## ⚙️ Смена модели:

Чтобы изменить модель, отредактируй `src/services/gemini.js`:

```javascript
// Строка 17:
const DEFAULT_MODEL = GEMINI_MODELS.FLASH_2; // Текущая

// Можно изменить на:
const DEFAULT_MODEL = GEMINI_MODELS.PRO_15; // Для более качественной генерации
```

---

**Готово! Бот переведен на Gemini.** 🎉
